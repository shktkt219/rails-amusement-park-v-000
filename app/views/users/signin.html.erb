<%= form_for @user, url: "/signin" do |f| %>
  <div class="field">
    <%= f.label :name, "Select your name" %><br>
    <%= f.collection_select(:user, :id, @users, :id, :name, :include_blank => true) %>
  </div>

  <div class="actions">
    <%= f.submit "Sign In" %>
  </div>
<% end %>
